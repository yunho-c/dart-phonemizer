```
╭───────────────────────────────────────────────────────────────────────────╮
│  > Could you create a minimal dart implementation of phonemization,      │
│    specifically using the `espeak-ng` backend (no need to set up complex │
│     modular backend structure unless it's better anyway)? Feel free to   │
│    use @references/phonemizer as reference (but no need to stick to how  │
│    it does things)                                                       │
╰───────────────────────────────────────────────────────────────────────────╯
```
